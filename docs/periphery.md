# Periphery — in-other-shops

This package contributes periphery to every consumer that installs it: auto-scheduled commands, event listeners, observers, subscribers, model boot hooks, the events it dispatches, and the public symbols consumers attach to and call. **This doc is the authoritative list of what this package adds to a consumer's runtime, plus the API surface consumers depend on.**

Consumers reference this file from their own `docs/periphery.md` rather than re-deriving it. The version that applies is whatever is pinned in their `composer.lock` — bump the package and the new version of this doc applies.

**Definition of done:** any change to a `*ServiceProvider.php`, package listener, observer, subscriber, model boot hook, scheduled command, dispatched event, **public `Has*` contract, `InteractsWith*` trait, public action signature, registry class method, Filament Schema static factory, or FlowChain step payload contract** must update the relevant section below and refresh "Last verified". The hook at `/home/jelte/projects/.claude/hooks/check-data-flows.sh` (when working from the workspace root) fires on edits to those files and reminds you.

Scope split: sections "Auto-scheduled commands" through "Tables owned by subsystem" cover the **runtime periphery** — what fires in a consumer's app when the package is installed. The "External surface" section at the bottom covers the **API contract** — what consumers depend on at the type level. Both are this package's contract with its consumers.

Last verified against codebase: 2026-07-02 (runtime sections refreshed for `SyncInventoryOnOrderStatusChange` in v0.23.0; External surface section added 2026-05-29 — initial population is opportunistic from CLAUDE.md, deepen on next audit; pricing:expire-compare-at row corrected via pathology-reviewer cross-check; 2026-05-29 pathology fixes applied — B-1 bulk-detach now dispatches `CategoryDetached`, B-2 attach/detach/move/delete now transactional, B-3 `morph_alias` widened to 255 + over-length guard, B-4 recompute advisory-locked + upsert, C-1 `UpdateOrderStatus` transactional, D-2 PricingLogSubscriber `commerce` channel confirmed intentional; 2026-05-29 second-pass — A-3/A-5 `PriceData` rejects orphan/past strikethrough, A-4 expiry also dispatches `PriceUpdated(fromExpiry)`, A-1 picker timezone pinned, B-6 `onMoved` re-derives from pivot, B-7 listener N+1 removed via `CategoryAncestry`, C-2 `UpdateOrderStatus` locks + idempotent same-status no-op; 2026-06-01 added `Commerce\Order\Actions\ResolvePreOrderAudience` + `PreOrderRecipient` DTO + `OrderLine::scopePreOrder()` to the External surface — pre-order engagement; 2026-06-01 `Taxonomy\Category` now implements `HasMedia` (cover image) — new Taxonomy→Media soft dependency, no migration (polymorphic `mediables` pivot, `category` morph alias already registered); `Taxonomy\Category` also now implements `HasTags` (intra-domain, no new cross-domain edge) for a `featured`-style category flag, no migration (`taggables` pivot, same `category` morph alias); 2026-06-02 **Variants domain Phase 1 landed** (unreleased) — new `Variants` subsystem (`options`, `option_values`, `variants`, `option_value_variant`, `optionables` tables; morph aliases `option`/`option_value`/`variant`); `HasVariants`/`InteractsWithVariants` contract+trait now ship; package `Variant` adopts `HasPrices`/`HasStock`/`HasMedia`, `Option`/`OptionValue` adopt `HasTranslations`; `VariantCreated`/`VariantDeleted` event classes exist but are not yet dispatched (Phase 2 actions); 2026-06-02 **Variants Phase 2 landed** (unreleased) — package `Variant` now implements `HasCart` (Variants→Commerce edge); **new `InteractsWithCart` `deleting` guard blocks deleting any cart-able referenced by a live cart** (config `commerce.cart.guard_cartable_deletion`, default true — affects Product/Bundle in both consumers, verify in-other-worlds before release); public actions `CreateVariant`/`GenerateVariants`/`CreateDefaultVariant`/`DeleteVariant` (the last three dispatch `VariantCreated`/`VariantDeleted`); `HasVariants` contract extended with `lowestVariantPrice`/`hasVariantInStock`/`variantStockTotal` (trait-defaulted, non-breaking); `CartReferencesCartableException` added. Storefront variant surfacing deferred — no consumer browses variants via the Storefront API; 2026-06-02 **Variants Phase 3 landed** (unreleased) — `OptionResource` (standalone admin for the global Option/OptionValue catalog, translated name + per-value labels; consumer-registered like other package resources) and `VariantsSchema` (`axesField`/`variantsRepeater`/`fillFormData`/`saveFormData`). Generate-combinations Filament action + owner-price cascade are consumer-side, deferred to Phase 5; 2026-06-02 **`Variants\OptionValue` adopts `HasMedia`** (unreleased, suggested v0.29.0) — an optional single **swatch** image per value (`swatch` collection) for the storefront variant picker, managed via a per-value `FileUpload` in `OptionResource`. No migration (polymorphic `mediables`, `option_value` alias already registered); no new dep edge (Variants→Media already exists); 2026-06-03 **gross-inclusive VAT + per-bracket tax landed** (unreleased) — `CalculateTotal` signature changed (per-line `taxRateBps`, no single `taxRate`), tax derived per rate bracket; new `CalculateIncludedTax` action, `TaxBreakdownLine` DTO, `TaxMode` enum, `PricingConfig::defaultTaxMode()` + `pricing.default_tax_mode` config; `PriceBreakdown`/`PriceBreakdownLine` gained tax fields (additive, but `subtotal` is now gross + `tax` is contained — breaking semantics); `orders.tax_summary` JSON column + `Order::taxSummary()`; `order_lines.tax_amount` now nullable + unpopulated, lines keep `tax_rate_bps`. Consumer `CalculateTotals`/`ResolveTaxRateForOrder` update + bianka TODO pending in the same release window; 2026-06-03 **Purchasing domain Phase 1 landed** (unreleased) — new `Purchasing` subsystem (`suppliers`, `purchase_orders`, `purchase_order_lines` tables; morph aliases `supplier`/`purchase_order`/`purchase_order_line`); `HasPurchases` (extends `HasStock`) + `InteractsWithPurchasing` contract+trait ship; actions `CreatePurchaseOrder`/`PlacePurchaseOrder`/`ReceiveItems`/`CancelPurchaseOrder`; events `PurchaseOrderCreated`/`PurchaseOrderPlaced`/`ItemsReceived`/`PurchaseOrderCancelled` routed through new `PurchasingLogSubscriber` (new `purchasing` log channel); receiving drops Inventory `StockMovement(Received)` referencing the purchase order line. Filament Resources + the `CommerceSchema`/`HasOrders` contract-discovery retrofit are the next phase — **not yet built**, so no consumer wires `HasPurchases` yet; 2026-06-03 **Purchasing Phase 2 landed** (unreleased) — admin UI: `PurchaseOrderResource` (+ pages; create maps to `CreatePurchaseOrder`, table actions Place/Receive/Cancel call the lifecycle actions) and `SupplierResource` (+ pages) under a new `Purchasing` nav group, plus `Purchasing\Filament\PurchasingSchema` whose line-item picker **discovers** `HasPurchases` models via the morph map (options keyed `{alias}:{id}`). 2026-06-03 **CommerceSchema discovery retrofit DONE** (unreleased, breaking) — `CommerceSchema::orderLinesRepeater()`/`orderLineFields()` **lost their `$orderableModels` + `$orderableTitleColumn` params** and now discover orderable models from the morph map (`HasOrders` implementers), keying options `{alias}:{id}` (fixes the bare-`id` cross-type collision). The `orderableModels()` override hook was **removed** from package `OrderResource` + `OrderLinesRelationManager`. Consumer impact: in-other-worlds' `OrderResource` does **not** override `orderableModels()` (only `currencyOptions()`), so no consumer break — but its order-create admin will newly show an orderable product picker (auto-discovered Product + Bundle) once the package is released. No live order data (reseed-safe). Consumer wiring (`Product`/`Bundle` adopt `HasPurchases`) is still **release-gated** (Packagist). NB consumer already has an unrelated `App\Contracts\Purchasable` meaning "sellable" — opposite direction to package `HasPurchases` ("buyable from supplier"); 2026-06-04 **Refund domain landed** (unreleased, on `feat/refund-domain`) — new `Commerce\Order\Models\Refund` (`refunds` table; morph alias `refund`; `Commerce::refund()` registry; soft `payment_id` ref + unique `(gateway, gateway_refund_id)`); `Order::refunds()`/`refundedTotal()`/`isRefunded()`/`isPartiallyRefunded()` (refunded-ness is derived, NOT an `OrderStatus` case); `RefundActor` DTO + `RefundActorSource` enum (admin|gateway) in `Commerce\Order`; new actions `ReverseTax` (cumulative-anchored per-bracket VAT reversal), `RecordRefund` (idempotent on `gateway_refund_id`, dispatches `RefundRecorded`), `RefundOrder` (admin orchestration: RefundPayment→RecordRefund→restock/cancel; two restock modes); new event `RefundRecorded` → `CommerceLogSubscriber::handleRefundRecorded` (commerce channel); **new listener `ReconcileRefundFromWebhook` on `PaymentRefunded`** (registered in `CommerceServiceProvider`) records the Refund row for gateway-initiated refunds. **Payment domain BREAKING:** `PaymentGateway::refund()` now returns `string` (gateway refund id, was void) — Stripe/Fake/test implementors updated; `RefundPayment::__invoke()` returns `RefundResult` (was Payment) and **no longer records or dispatches** (recording moved to Commerce); `PaymentRefunded` event gained `?gatewayRefundId`+`?refundAmount` and is now dispatched ONLY by the webhook (not the admin path); `WebhookPayload` gained `?amountRefunded`+`?gatewayRefundId`; `ProcessPaymentWebhook` now handles `charge.refunded`/`charge.refund.updated` (locks the payment, `amount_refunded` monotonic max, status recomputed from amounts) — closes F27/F30/F31; `StripePaymentGateway::parseWebhook` branches on event type (Charge/Refund shapes, no longer reads everything as a PaymentIntent). Filament: two order-level actions on the package `OrderResource` EditOrder page ("Partial refund" w/ reservation-restock picker, "Refund & cancel order") call `RefundOrder`; the per-payment refund action was **removed** from `PaymentsRelationManager` (now display-only); a "Refunded"/"Partial" badge on the order table + a persistent warning on a refunded order's edit page (soft F28 guard). Also in this branch (Wave 1 audit fixes): `ReleaseReservation::__invoke()` gained an optional `$description` (release reason, F15); `Cart::items()` now `orderBy('id')` (G6); `CreateOrder` asserts `sum(line_total) == subtotal` when the breakdown carries lines (T1/G2, throws `CommerceException::subtotalMismatch`); `CalculateTax` docblocked exclusive-only (G12). **Consumer impact (release-gated, Packagist):** in-other-worlds `OrderResource` (extends the package one) gains the two refund actions + the refund badge automatically; no consumer code change needed beyond the version bump); 2026-06-04 **VAT-edge fixes G4/G5 landed** (unreleased, on `feat/refund-domain`, non-breaking) — `CalculateTotal` now extracts the VAT contained in `shippingCost` (apportioned across goods brackets by gross share, EU ancillary rule) so `order.tax`/`tax_summary` no longer under-collect on shipped orders; per-bracket discount allocation switched to largest-remainder (was floor-and-dump on the highest-rate bracket). New shared `Pricing\Support\LargestRemainderAllocator` (also now backs `ReverseTax`). No signature/DTO-shape change; `order.tax` value rises on shipped orders — consumer checkout tax assertions must be re-swept at release (in-other-worlds `CheckoutControllerTest` mixed-rate cases)); 2026-06-04 **audit fixes G10/G7/G3/F18/G9 landed** (unreleased, on `feat/refund-domain`) — G10 `LogDispatcher` now isolates each handler (a throwing audit write degrades to the app log instead of 500ing the committed action) + `DatabaseLogHandler` encodes context safely (no more literal `false`); G7 `AdjustStock` locks shared-inventory group members in a deterministic key order (no cross-sibling deadlock); G3 new `AuditPipelineRowTest` asserts real domain_logs rows for the stock channels (purchasing/inventory); F18/G9 **new `inventory:reconcile` command** (registered-but-not-scheduled, read-only) — see the commands table); 2026-06-04 **checkout-resilience P1 landed** (unreleased, on `feat/refund-domain`, BREAKING contract) — **`PaymentGateway::cancelSession(Payment): void` added to the contract** (every gateway impl must add it; cancels the intent so a late payment can't land on an abandoned order — used by order-expiry); Stripe `createSession` now sends an **idempotency key = payment id** (a retried session-open returns the same intent, no duplicate charge); `InitiatePayment` decomposed into **`CreatePendingPayment`** (persist a Pending payment, no gateway call — txn-safe) + **`OpenPaymentSession`** (the gateway call — runs after commit), with `InitiatePayment` composing both (signature unchanged); new `PaymentNotCancelableException` (thrown by `cancelSession` when the intent is live). Part of the F1 persist-then-pay redesign — see `docs/briefs/checkout-resilience-brief.md`); 2026-06-04 **checkout-resilience P2 landed** (unreleased) — new `Commerce\Order\Actions\ConfirmOrder` (idempotent confirm: locks the order, returns `ConfirmOrderOutcome` enum `Confirmed`/`AlreadyConfirmed`/`StockUnavailable`/`NotConfirmable`; F14 guard refuses to confirm an order whose reservations were released); new `OrderConfirmationBlocked` event (paid-but-unconfirmable) → `CommerceLogSubscriber::handleOrderConfirmationBlocked` (commerce channel, Warning). Consumers may subscribe to `OrderConfirmationBlocked` to alert an operator. Fixes the F2/F3 duplicate-confirmation bug: the caller fires the buyer email/cart-clear only on the `Confirmed` outcome, so redelivery (`AlreadyConfirmed`) no longer re-emails); 2026-06-05 **audit-log actor attribution landed** (unreleased, on `feat/audit-actor-attribution`, additive — F21/F22 ~v0.33.0) — every `domain_logs` row now records *who* via a structured `LogActor` (`Logging\DTOs\LogActor` + `Logging\Enums\LogActorType` User/Gateway/System/Agent). New nullable columns `actor_type`/`actor_id`/`actor_label` (migration `2026_06_05_..._add_actor_to_domain_logs_table`; pre-existing rows read unknown, no backfill). The actor is set **once at each boundary** in `LogContext` (new `setActor`/`actor`/`forgetActor`) and the dispatcher resolves precedence **explicit (`LogEntry.actor`) > ambient (`LogContext`) > `LogActor::unknown()`** — so no row is anonymous and no event/action signature threads an actor. Boundaries that set it: payment webhook (`ProcessPaymentWebhook` → gateway; **constructor gained `LogContext`**), scheduled commands `commerce:expire-orders`/`inventory:release-expired`/`pricing:expire-compare-at` (→ system, via new `Logging\Concerns\RunsAsSystemActor`), agent `/mcp` (`AuthenticateAgent` → agent; **constructor gained `LogContext`**); the refund log path derives a `LogActor` from the durable `RefundActor` (admin→user, gateway→gateway) and passes it explicitly. `LogEntry` gained `?LogActor $actor = null` (additive). **Consumer side (P4, separate):** a `SetAuditActor` web/admin middleware calls `LogContext::setActor(LogActor::user|guest())` — so `Logging\LogContext` + `LogActor` are now consumer-facing surface (see below); 2026-06-05 **G9 prevention landed** (on `fix/reserve-default-ttl`, behavioural, non-breaking) — `ReserveStock` now applies the documented `inventory.reservation_ttl` default when a caller omits `reservedUntil`, instead of silently passing `null` through. A Pending reservation with no TTL is invisible to `inventory:release-expired` forever (its `Reserved` decrement leaks → stock drifts down), and the config's own comment already promised this default but the code never honoured it. Signature unchanged; an explicit `reservedUntil` still wins; `inventory.reservation_ttl = null` is the deliberate permanent-hold opt-out (still surfaced by `inventory:reconcile`). Consumer `ReserveItems` already passes an explicit TTL, so no consumer behaviour changes; the fix protects every other reserve path (admin tooling, future API, agent/Variants)); 2026-06-05 **blind pathology re-review fixes landed** (unreleased, on `fix/inventory-refund-pathology`) — `ReleaseReservation::__invoke()` gained `bool $onlyIfExpired = false` (additive); the `inventory:release-expired` cron now passes it so a Pending+expired reservation that was confirmed (paid) in the snapshot→lock gap is **not** released (oversell-after-payment guard — distinct from F14's release-then-late-pay, which the round-2 ExpireAbandonedOrders reframe didn't cover for the standalone reservation-expiry cron). Also `StockCast` docblock corrected — it claimed a non-existent read-side int cast; reads are raw and `isLowStock` leans on PHP numeric coercion. `LargestRemainderAllocator` cap-underallocation assessed non-triggerable (all capped callers pass num≤den). **2026-06-06 follow-up (H8) landed:** `ExpireAbandonedOrders` no longer holds the order lock across the Stripe `cancelSession` — gateway cancellation runs in an unlocked phase 1 (cancel-first ordering preserved: a live intent still aborts the expiry), then a short locked phase-2 transaction does the re-check + Pending→Cancelled transition. Observable behaviour unchanged (all `ExpireAbandonedOrdersTest` cases green); no signature/surface change. **Still flagged (need a decision):** F34 residue — `RefundPayment` holds the payment row lock across the Stripe refund call and writes `amount_refunded` inside the gateway txn → gateway-success-then-DB-fail still possible (backstopped by the `charge.refunded` webhook reconciliation + gateway cap); 2026-06-11 **package-tightening Phase 1 landed** (unreleased, on `chore/package-tightening`, see `docs/briefs/package-tightening-brief.md`) — **new `currency.enabled` config** (`src/Currency/config/currency.php`, merged in `CurrencyServiceProvider::register()`, publishable as `currency-config`): ISO-code allowlist read by `Currency::enabled()`; the key previously had no config file anywhere, so it could never load and `enabled()` always returned all cases. The never-read `pricing.currencies` key was **removed** (split-brain twin). **`Voucher::validateForUse(int $subtotal, Currency $currency): void`** is new public model surface — the single redemption guard (validity window / minimum order / fixed-voucher currency match), extracted from the previously duplicated private blocks in `CalculateVoucherDiscount` + `ApplyVoucher`; both actions now delegate (ApplyVoucher still calls it under its row lock — TOCTOU semantics unchanged). Agent tools: triplicated `resolveStockableClass()` extracted to `Agent\Support\ResolveStockableModel`, pagination clamping to `Agent\Support\PaginationParams` (per-tool limits unchanged; **`list_orders` now floors `per_page` at 1** where 0/negative previously reached `paginate()` — invalid input degrades instead of erroring). EditOrder `afterFill` + the order-table refund badge each compute `refundedTotal()` once instead of via the two-query predicate pair (display-only, same derivation); 2026-06-11 **package-tightening Phase 2 landed** (unreleased, same branch) — new **`InOtherShops\Support\` namespace** (first cross-domain namespace; rule: domain-agnostic mechanics only). **`Support\DomainServiceProvider`** base now underlies 12 providers (Location, Media, Translation, Tax, Variants, Shipping, Purchasing, Pricing, Inventory, Taxonomy, Commerce, Logging) — identical register/boot effects, hook-declared (domainDir/configKey/morphAliases/logSubscriber/domainCommands/publishesConfig + scheduleWhenEnabled); Currency/Storefront/Payment/Agent/FlowChain/Stripe stay bespoke. Runtime behavior unchanged (same merges, morph maps, subscribers, commands, publish tags, schedule gates) — guarded by new `BootConfigMergeTest` (one package-default config key per domain on a bare boot). **`Support\Filament\MoneyFields`** (moneyInput/formatCents/dehydrateCents/dehydrateBps/percentLabel) + **`BackedEnumState`** now back every money/percent transform (PricingSchema::moneyField delegates — consumer-facing signature unchanged; PurchasingSchema, PurchaseOrderResource, CommerceSchema, OrderResource, VoucherResource, TaxRateResource migrated; null-dehydration policies preserved per column nullability, pinned by `MoneyFieldsTest`). **One visible change (D2): TaxRate admin rate column now renders `21%` not `21.00%`.** **`Support\Filament\{PackageListRecords,PackageEditRecord}`** base pages: 10 List + 10 Edit package pages re-parented (default Create/Delete header actions in the base; EditOrder/EditCategory keep custom headers); 3 empty `getRelations()` overrides deleted (Filament base already returns []); census pinned by `PackagePageBasesTest`. **`Logging\LogSubscriberBase`** (in Logging, not Support — inseparable from Logging types): all 8 audit subscribers re-based, handlers call `$this->log(level, message, context, ?actor)`; channels stay per-subscriber constants (Pricing→commerce intentional exception unchanged); LogEntry output byte-identical; 2026-06-11 **package-tightening Phase 3 landed** (unreleased, same branch) — **`Support\Transitionable` interface + `Support\StateTransitions` trait** now back `OrderStatus`/`ShipmentStatus`/`PurchaseOrderStatus` (label/color/allowedTransitions + shared canTransitionTo; PaymentStatus deliberately excluded — gateway-driven, no local state machine). **BREAKING: `Shipping\Concerns\InteractsWithShippability` removed** (D5/WI-10) — its entire body was a constant `return true`; the `HasShippability` contract is unchanged and load-bearing (`Cart::requiresShipping()` instanceof-checks it). Consumer impact: in-other-worlds `Product` had its own digital-aware `requiresShipping()` shadowing the trait all along, so the consumer fix is import-removal only (+ new `ProductShippabilityTest` guarding the contract wiring, since Cart's non-implementor fallback of `true` would mask a dropped interface); bianka unaffected); 2026-06-11 **v0.37.0 locale-aware money display** — `Currency::format()`/`formattedAmount()`/`percentLabel()` gained optional `$locale` params (additive), new `DisplayLocale` + `MoneyFormatter` support classes, new opt-in `SetMoneyDisplayLocale` panel middleware, new `currency.display_locale` config key (ships null = follow app locale), `ext-intl` now an explicit requirement — see External surface → Currency display surface; 2026-06-14 **manual-sync dehydration hardening** (unreleased, on `fix/manual-sync-dehydrated-state`) — new `Support\Filament\SyncsManualFormState` trait (Filament page helper; see External surface → Filament page helpers) so Create/Edit pages feed manual-sync schemas the *dehydrated* form state, not raw `$this->data` (a Filament 5 RichEditor's raw state is a TipTap doc array, not the HTML string the `translations.value` column stores → "Array to string conversion" + orphan row). `TranslationSchema::saveFormData` now **throws `InvalidArgumentException` on a non-scalar value** (loud guard, no TipTap coupling) instead of corrupting/500ing. Consumer adoption release-gated: bianka Product pages + (planned) all in-other-worlds manual-sync pages; 2026-06-14 **Purchasing incoming-stock helpers + line-description relax** (unreleased) — `InteractsWithPurchasing` gained two trait-only helpers (not on the `HasPurchases` contract): **`incomingPurchaseLines(): MorphMany`** (purchase lines scoped to Ordered/PartiallyReceived orders; eager-load `purchaseOrder` for a breakdown) and **`incomingQuantity(): int`** (Σ outstanding qty across open orders). Additive — every implementer already using the trait gets them free. Also `PurchasingSchema::purchaseLineFields()` line **`description`** is now a `Textarea` (rows 2, columnSpanFull) and **no longer `->required()`** — a product-backed line still snapshots its description, and the CreatePurchaseOrder action remains the real "description-or-purchasable" guard. in-other-worlds consumes `incomingQuantity`/`incomingPurchaseLines` in the Product admin's new Supplier tab; 2026-06-18 **`SyncCategories` action + `categoriesSelect` now maintains counts** (v0.40.0) — new public action **`Taxonomy\Actions\SyncCategories(Model&HasCategories $model, array $categoryIds): void`** (the set-operation sibling to AttachCategory/DetachCategory): diffs current-vs-given and routes each add/remove through those actions so `CategoryAttached`/`CategoryDetached` fire and `MaintainCategoryCounts` keeps `category_morph_counts` current; the whole set-change is wrapped in one transaction. **`TaxonomySchema::categoriesSelect()` now overrides Filament's default relationship sync (`saveRelationshipsUsing` → `SyncCategories`)** — fixes a silent bug where admin category assignment wrote the pivot directly and never updated the counts table, so the category nav / table-of-contents read empty. Behaviour change to a Filament Schema static factory: consumers' admin category saves now fire the events (in-other-worlds + bianka — sweep both before release). Pinned by `SyncCategoriesTest`. Companion app-side fix in in-other-worlds: the MCP `create_content_draft`/`update_content_draft` tools route categories through `AttachCategory`/`DetachCategory` for the same reason (raw `->attach()`/`->sync()` there had the identical bypass — and was the path that produced the first empty live ToC); 2026-06-18 **`taxonomy:reconcile-category-counts` tripwire** (v0.41.0) — new read-only command + `Taxonomy\Actions\ReconcileCategoryCounts(): CategoryCountReport` action + `Taxonomy\DTOs\CategoryCountReport` (drift list, `isClean()`/`issueCount()`): reports (never repairs) drift between `category_morph_counts` and the `categorizables` pivot rollup — missing rows (an attach bypassed the events, the empty-ToC symptom), orphan rows, wrong counts — exits non-zero + `Log::warning`s so a scheduled run alerts. Repair stays `taxonomy:recompute-category-counts`. Both now read the **shared `Taxonomy\Support\CategoryCountAggregator::expected()`** (extracted from recompute's rebuild) so detector and repair can't disagree about "correct". Registered-but-not-scheduled (per the `inventory:reconcile` precedent); in-other-worlds schedules it. New External-surface: `ReconcileCategoryCounts` action + `CategoryCountReport` DTO. Pinned by `ReconcileCategoryCountsTest`; 2026-06-18 **`MediaSchema` per-collection cover opt-out** (unreleased) — new optional `media.collections.*.cover` config flag (default true) + new public `MediaSchema::collectionAllowsCover(string): bool`. A collection holding non-image media (video embeds, documents) sets `cover => false`: the `mediaRepeater` then omits the "Use as cover image" toggle and `saveFormData`/`normalizeSingleCover` force `is_cover = false` for that collection regardless of submitted state. Fixes a broken homepage image where an editor flagged a YouTube `embed` row as the site cover and `Media::url()` (the raw watch URL) was rendered as `<img src>`. Package default now marks `documents` non-cover. Consumer wiring (release-gated): in-other-worlds sets `cover => false` on its `embed` collection. Pinned by new `MediaSchemaTest` cases; 2026-06-22 **`CategoryResource` admin improvements** (unreleased) — two new Taxonomy actions, both **internal to the package admin** (not in the consumer-called table): **`Taxonomy\Actions\ListCategoriesInTreeOrder(): list<int>`** returns category ids in depth-first parent→child order (siblings by position, id), now driving the `CategoryResource` index default sort so the flat Filament table reads as a tree (parent first, children beneath); applied as `defaultSort` so a column-header click still wins and drag-reorder bypasses it. **`Taxonomy\Actions\ListCategoryAttachments(Category): array<string, list<string>>`** reads the `categorizables` pivot, groups attachments by morph type, and labels each via a generic `name → title → "{type} #id"` fallback — **deliberately not** `HasStorefrontPresence` (would create a Taxonomy→Storefront cycle); it backs a new read-only "Assigned items" section on the Category edit page (hidden on create) listing assigned products/bundles/content grouped by type. No new dep edge, no migration, no consumer wiring needed (admin-only behaviour ships with the version bump). Pinned by `ListCategoriesInTreeOrderTest` + `ListCategoryAttachmentsTest`; 2026-06-22 **`CategoryResource` "Assigned items" — subtree rollup + clickable links** (v0.44.0, unreleased) — `ListCategoryAttachments` now **rolls up the whole subtree** (items attached to the category *and any descendant*, matching how the storefront counts roll up to ancestors — opening a parent no longer reads empty when content is filed on children) and its **return shape changed** from `array<string, list<string>>` to `array<string, list<array{id:int, label:string, filedUnder:?string}>>` (`filedUnder` = the descendant category names an item sits under, null when attached directly). Still internal to the package admin — not consumer-called. The "Assigned items" section now renders each item as a **link to its admin edit page**, resolved generically via `Filament::getModelResource($modelClass)` + `getUrl('edit', …)` (falls back to plain text when no resource/edit page) — so the package links to consumer-defined resources (Product/Content/Bundle) without importing them; no new dep edge. Pinned by updated `ListCategoryAttachmentsTest` (rollup/filedUnder/grouping/fallback cases); 2026-07-02 **T-SEC2 default-deny Filament authorization** (unreleased) — new `Support\Filament\PackageResource` base (`$shouldCheckPolicyExistence = false`); all 10 package Filament Resources re-parented onto it (CustomerGroup/Customer/Order, Voucher, PurchaseOrder/Supplier, TaxRate, Category/Tag, Option). A package Resource with no consumer policy now **denies** instead of Filament's stock silent-allow; `Gate::before` super-admin bypass preserved; strict-mode compatible. New External-surface subsection "Filament Resource authorization" lists the required consumer policy mappings. Pinned by `PackageResourceAuthorizationTest` (deny/allow/before-bypass behavioural cases + a census that every shipped Resource extends the base). Consumer impact (release-gated): in-other-worlds already ships the nine matching policies — no change beyond the version bump; a Variants consumer (bianka) must add an `Option` policy; 2026-07-02 **T-SEC1 harden agent admin elevation** (unreleased) — new `agent.auth.oauth.admin_client_ids` config key (env `AGENT_OAUTH_ADMIN_CLIENT_IDS`, comma-separated, default empty). `AuthenticateAgent` now grants `is_admin` on the OAuth path only when the token carries the admin scope **AND** its client is confidential + (first-party OR allowlisted) — the scope alone no longer elevates (`clientMayElevate`, fails closed). Static-bearer operators are unaffected (still admin). Reachability check: in-other-worlds registers only `agent` in `Passport::tokensCan()` — `agent.admin` is not interactively grantable there, so the escalation was latent; the gate is defense-in-depth. Consumer impact (release-gated): none today; if in-other-worlds later provisions an admin OAuth client it must be a confidential first-party client or be pinned in `admin_client_ids`. Pinned by new OAuth-path cases in `AuthenticateAgentMiddlewareTest`; 2026-07-02 **T-M0 dead-code removal** (unreleased) — two ⚠EXT contract-method removals: **`HasMedia::mediaInCollection(string): Collection`** (+ its `InteractsWithMedia` impl) and **`HasPrices::priceCurrencies(): array`** (+ its `InteractsWithPrices` impl) deleted — neither consumer (in-other-worlds, bianka) called either (grep-verified), single-release-window safe. Also removed (non-surface): `InteractsWithTranslations::translationsFor()`, an unused `$quantity` param on `CreateOrder::resolveLineData` (private), a redundant `?? ''` re-coalesce in `StoreMedia::attachToModel` (private, param tightened to non-null `string`), an unreachable `status !== Pending` recheck in `ConfirmReservation` (private, the locked query already filters Pending), redundant bare `categories`/`tags` eager-load keys in `Storefront\ResolvesEagerLoading` (the nested `*.translations` keys already load the parent), and the empty `src/Storefront/Http/` directory. Domain READMEs (Media/Pricing/Translation/Commerce) updated to drop the removed methods; 2026-07-02 **Wave 3 Agent/Pricing DRY (T-A1..A7)** — mostly internal, two periphery-relevant: **T-A6 (⚠EXT, additive)** `AgentTool::title()` now wires to `displayName()`, so MCP `tools/list` advertises each tool's human-readable title (new optional field, non-breaking — pinned by `McpEndpointTest`); **T-A5 (runtime)** the shipped `domain-log.php` now maps the `shipping` channel (the `ShipmentLogSubscriber` already wrote to it, but the missing map routed shipping audit lines to the `daily` default — now they land in their own `shipping` file channel; pinned by `DomainLogChannelMapTest` + the corrected `ShippedDefaultConfigTest` census). Internal-only (no surface): `Agent\Support\ResolveBrowsableModel` extracted + composed by `ResolveStockableModel` (unified unknown-type error string), `AgentTool::failure()`/`paginationMeta()` helpers, `ListBrowsables` per_page floored at 1, `UpdateCartItemQuantity` delegates removal to `RemoveFromCart`, `PriceData::attributes()` column map shared by Create/UpdatePrice. **Flagged decision:** zero-caller `Agent\Agent` facade left in place pending Jelte's delete call; 2026-07-02 **T-E1 HasLabel enum trait** (⚠EXT, admin labels) — new `Support\HasLabel` trait (sentence-case default) now backs `label()` on 8 string-backed enums (OrderStatus, ShipmentStatus, PurchaseOrderStatus, PaymentStatus, ReservationStatus, StockMovementReason, RefundActorSource, AddressType). One visible label change: `PaymentStatus::PartiallyRefunded` renders **"Partially refunded"** (was Title-Case "Partially Refunded") — the lone outlier corrected; no data/UI depends on it pre-launch. `AddressType::ShippingAndBilling` keeps its "Shipping & Billing" override. Convention flagged in CLAUDE.md. Pinned by `HasLabelTest`; 2026-07-02 **T-S2 NavigationGroup enum** — `Support\Filament\NavigationGroup` (one case per domain, no "Shop" catch-all); all 10 package Filament Resources report an enum group (Pricing/Tax/Variants moved off the shared "Shop" group to their own). Pinned by `NavigationGroupTest`. 2026-07-02 **T-B-MIG1 status macro** — **new auto-registered `Support\SupportServiceProvider`** (first in the provider list) that registers a `$table->status()` Blueprint macro (`string(30)` + single index, `index: false` opt-out); the 5 status columns (orders/payments/shipments/stock_reservations/purchase_orders) migrated in place (pre-launch, no data). The provider registers only the macro — no runtime actors, no scheduled work. Convention flagged in CLAUDE.md; pinned by `StatusColumnMacroTest`. 2026-07-02 **T-S-PROVIDER SP-1** — `PaymentServiceProvider` re-parented onto `Support\DomainServiceProvider` (runtime byte-identical: same config merge, migrations, morph map, `payment-config` publish, command, and `PaymentLogSubscriber`; the `PaymentGatewayManager` singleton kept as the one bespoke `register()` override). Guarded by `BootConfigMergeTest`

---

## Auto-scheduled commands

These commands register themselves into the consumer's Laravel scheduler via `app->booted()` in their ServiceProvider. They fire automatically whenever the consumer has a `schedule:run` cron wired on the host. Each is gated by a config key with a default of `true` — consumers can disable individual schedules without losing the command itself.

| Command | Cadence | Subsystem | Config gate (default) | Effect |
| --- | --- | --- | --- | --- |
| `inventory:release-expired` | every 5 min | Inventory | `inventory.schedule.enabled` (`true`) | finds `stock_reservations` with `status='pending'` and `expires_at < now()`, releases them (deletes the reservation row, creates a release `stock_movements` entry), dispatches `ReservationReleased` |
| `pricing:expire-compare-at` | hourly | Pricing | `pricing.schedule.enabled` (`true`) | finds `prices` where `compare_at_until < now()` and `compare_at_amount` is not null, **promotes `compare_at_amount` into `amount`**, then nulls both `compare_at_amount` and `compare_at_until`. Dispatches `CompareAtPriceExpired` (carries the pre-promotion amount) **and `PriceUpdated` with `fromExpiry: true`** (generic amount-change signal for cache/index/denorm consumers; the log subscriber skips the generic line for this path). The amount rewrite means `prices.amount` is effectively owned by this scheduler on the hour — other writers must coordinate (see also: cron-owned-column concurrency in pathology-reviewer rulebook) |
| `logging:prune-domain-logs` | daily | Logging | `domain-log.schedule.enabled` (`true`) | deletes `domain_logs` rows older than `config('domain-log.retention_days')` (default 90) |

## Console commands (registered, not auto-scheduled)

Registered via `$this->commands([...])` but not scheduled — consumers wire them into their own `routes/console.php` if they want them to run periodically.

| Command | Subsystem | Purpose |
| --- | --- | --- |
| `inventory:reconcile` | Inventory | **read-only tripwire (unreleased)** — reports (never repairs) stock-level drift (`stock_level != sum(movements)`, F18), pending reservations with no TTL (invisible to the expiry cron, G9), and pending reservations past their TTL (cron not running, T6). **Exits non-zero on any drift** and `Log::warning`s a summary, so a scheduled run surfaces problems to monitoring. Not auto-scheduled — consumers wire it into their own scheduler + alerting. |
| `purchasing:reconcile-receipts` | Purchasing | **read-only tripwire (unreleased)** — reports (never repairs) purchase-order lines whose `quantity_received` counter diverges from its `Received` stock-movement ledger (G11). Exits non-zero on drift + `Log::warning`s. Not auto-scheduled. |
| `commerce:expire-orders` | Commerce | **(unreleased)** cancels Pending orders past `commerce.order.abandon_after_minutes` (default 60) with no succeeded payment — releases their reservations (via the Cancelled transition) and **cancels the gateway intent** so a late payment can't land (F14). Aborts an order whose intent is already live. `--minutes=N` override. **Not auto-scheduled** (it cancels orders + calls the gateway) — consumers schedule it explicitly. |
| `commerce:prune-carts` | Commerce | deletes guest carts with `expires_at <= now()` (no-op if consumer never stamps `expires_at` on carts) |
| `payment:prune-webhook-events` | Payment | deletes `webhook_events` older than `config('payment.webhook_retention_days')` (default 90); accepts `--days=N` |
| `taxonomy:recompute-category-counts` | Taxonomy | rebuilds `category_morph_counts` denormalization table from scratch; **recovery command — use only if counts drift**. Takes an advisory lock (MySQL `GET_LOCK` / Postgres `pg_try_advisory_lock`; no-op on SQLite) so two runs can't collide, and writes the rebuild via upsert so a concurrent attach/detach during recovery can't crash it on a duplicate key. Now derives its rows from the shared `Support\CategoryCountAggregator::expected()` (same truth function the reconcile tripwire reads) |
| `taxonomy:reconcile-category-counts` | Taxonomy | **read-only tripwire (v0.41.0)** — reports (never repairs) drift between `category_morph_counts` and the `categorizables` pivot rollup: missing rows (an attach bypassed `CategoryAttached` — the empty-ToC bug), orphan rows (count with no pivot basis), and wrong counts. **Exits non-zero on any drift** + `Log::warning`s; repair is `taxonomy:recompute-category-counts`. Not auto-scheduled — consumers wire it into their own scheduler + alerting (in-other-worlds schedules it) |
| `flowchain:publish` | FlowChain | publishes a chain definition from the package into the consumer app (dev/ops only) |
| `flowchain:list` | FlowChain | lists all registered chains (ops) |
| `flowchain:check-contracts` | FlowChain | validates step payload contracts against `initialPayloadShape()` (dev) |
| `flowchain:verify-tests` | FlowChain | verifies test coverage of chains (dev) |

## Event subscribers

Auto-subscribed by their domain's ServiceProvider via `Event::subscribe(...)`. Each routes its subsystem's events through `LogDispatcher` to the `domain_logs` table.

| Subscriber | Subsystem | Listens to | Writes |
| --- | --- | --- | --- |
| `AgentLogSubscriber` | Agent | `ToolInvoked`, `ToolInvocationFailed`, `DynamicClientRegistered` | `domain_logs` (agent channel) |
| `CommerceLogSubscriber` | Commerce | `CartUpdated`, `CartClaimed`, `CartCleared`, `OrderCreated`, `OrderStatusChanged` | `domain_logs` (commerce channel) |
| `FlowChainLogSubscriber` | FlowChain | `FlowChainStarted`, `FlowChainCompleted`, `FlowChainFailed`, `FlowChainStepFailed`, `FlowChainStepCompleted` | `domain_logs` (flowchain channel) |
| `InventoryLogSubscriber` | Inventory | `StockAdjusted`, `StockReleased`, `StockReservationFailed`, `ReservationCreated`, `ReservationConfirmed`, `ReservationReleased` | `domain_logs` (inventory channel) |
| `PaymentLogSubscriber` | Payment | `PaymentSucceeded`, `PaymentFailed`, `PaymentRefunded` | `domain_logs` (payment channel) |
| `PricingLogSubscriber` | Pricing | `PriceCreated`, `PriceUpdated`, `PriceDeleted`, `CompareAtPriceExpired`, `VoucherApplied` | `domain_logs` (**`commerce` channel — intentional**, per audit D-2: price changes are commercial audit events and stream alongside orders/payments; there is no separate `pricing` channel in the default Logging config. The one deliberate exception to the per-domain channel convention) |
| `PurchasingLogSubscriber` | Purchasing | `PurchaseOrderCreated`, `PurchaseOrderPlaced`, `ItemsReceived`, `PurchaseOrderCancelled` | `domain_logs` (purchasing channel) |
| `ShipmentLogSubscriber` | Shipping | `ShipmentCreated`, `ShipmentReady`, `ShipmentDispatched`, `ShipmentDelivered`, `ShipmentReturnedToSender`, `ShipmentLost` | `domain_logs` (shipping channel) |
| `MaintainCategoryCounts` | Taxonomy | `CategoryAttached`, `CategoryDetached`, `CategoryMoved`, `CategoryDeleted` | `category_morph_counts` (incremental delta updates; rejects morph aliases over 255 chars with `MorphAliasTooLongException` — an unregistered FQCN morph map. The Attach/Detach actions and `Category::save()`/`delete()` wrap the pivot/lifecycle write and this walk in one transaction so a failed delta rolls the write back too) |

## Event listeners

| Listener | Subsystem | Trigger | Effect | Gate |
| --- | --- | --- | --- | --- |
| `CreateShipmentForNewOrder` | Shipping (registered in Commerce SP) | `OrderCreated` | calls `CreateShipment` action — writes `shipments`, `shipment_items` (one Pending shipment per order, using the order's snapshotted shipping method) | `shipping.auto_create_shipment` (default `true`) |
| `SyncInventoryOnOrderStatusChange` | Commerce | `OrderStatusChanged` | on `Pending → Confirmed`: calls `ConfirmReservation` to flip all pending reservations to Confirmed. On `* → Cancelled`: releases all pending or confirmed reservations via `ReleaseReservation`, restoring stock. Status-guarded actions make it safe to run alongside a consumer payment-success/fail listener that also calls them. | none — always on |
| `ReconcileRefundFromWebhook` | Commerce | `PaymentRefunded` | records the Commerce `Refund` row for a gateway-initiated refund (payment webhook) via `RecordRefund` (gateway actor), reversing the per-bracket VAT. Idempotent on `gateway_refund_id`, so a webhook echoing an admin refund no-ops. Only acts when the payment's payable is an `Order` and the event carries a refund id + amount. | none — always on |

## Model observers

| Observer | Model | Triggers | Effect |
| --- | --- | --- | --- |
| `CategoryObserver` | `Taxonomy\Category` | `updated` (parent_id change) | dispatches `CategoryMoved` |
| `CategoryObserver` | `Taxonomy\Category` | `deleting` | validates no children remain, dispatches `CategoryDeleted` |

## Model boot hooks (`static::saving` / `::deleting` / etc.)

These live inside model classes themselves, not in observers.

| Model | Hook | Effect |
| --- | --- | --- |
| `Media\Models\Media` | `deleting` | if `type=Upload`, deletes the physical file from disk via `Storage::disk($media->disk)->delete($media->path)` |
| `Pricing\Models\Price` | `saving` | validates `compare_at_amount > amount`; throws `InvalidCompareAtPriceException` if violated |
| **Any model using `Commerce\Cart\Concerns\InteractsWithCart`** (Variant + every consumer cart-able: Product, Bundle, …) | `deleting` | blocks deletion when a **live** cart (`expires_at` null or future) references it — throws `CartReferencesCartableException`. Gated by `commerce.cart.guard_cartable_deletion` (default true). Affects both consumers; expired guest carts don't block (they're pruned) |

## Events dispatched (public surface — consumers may subscribe)

State changes the package emits. Past-tense, `final readonly class`, `Dispatchable` trait. Listed by domain.

**Agent.** `ToolInvoked`, `ToolInvocationFailed`, `DynamicClientRegistered`.

**Commerce.** `CartUpdated`, `CartClaimed`, `CartCleared`, `OrderCreated`, `OrderStatusChanged`.

**FlowChain.** `FlowChainStarted`, `FlowChainCompleted`, `FlowChainFailed`, `FlowChainStepCompleted`, `FlowChainStepFailed`.

**Inventory.** `StockAdjusted`, `StockReleased`, `StockReservationFailed`, `ReservationCreated`, `ReservationConfirmed`, `ReservationReleased`.

**Media.** `MediaStored`, `MediaDeleted`. *(No package subscriber yet — admin-activity logging deferred until multi-user.)*

**Payment.** `PaymentSucceeded`, `PaymentFailed`, `PaymentRefunded`.

**Pricing.** `PriceCreated`, `PriceUpdated` (fires on every `prices.amount` change including the scheduled strikethrough promotion, where `fromExpiry: true`), `PriceDeleted`, `CompareAtPriceExpired`, `VoucherApplied`. A consumer wiring cache-bust / search-reindex / denormalized-price listeners should subscribe to `PriceUpdated` alone — it covers both direct edits and scheduled promotions. `CreatePrice`/`UpdatePrice` reject an orphan or past-dated strikethrough at the `PriceData` boundary (`InvalidCompareAtPriceException`); direct model writes (the expiry sweep, factories) bypass that guard deliberately.

**Purchasing.** `PurchaseOrderCreated`, `PurchaseOrderPlaced`, `ItemsReceived`, `PurchaseOrderCancelled`. Logged via `PurchasingLogSubscriber` (purchasing channel). `ItemsReceived` carries the per-line received quantities; the resulting stock deltas surface separately as Inventory `StockAdjusted` events (reason `Received`, the movement's polymorphic `reference` points at the `purchase_order_line`).

**Shipping.** `ShipmentCreated`, `ShipmentReady`, `ShipmentDispatched`, `ShipmentDelivered`, `ShipmentReturnedToSender`, `ShipmentLost`.

**Taxonomy.** `CategoryAttached`, `CategoryDetached`, `CategoryMoved`, `CategoryDeleted`, `TagAttached`, `TagDetached`. *(No package LogSubscriber — only `MaintainCategoryCounts` listens.)*

**Variants.** `VariantCreated` (dispatched by `CreateVariant`, and thus by `GenerateVariants`/`CreateDefaultVariant`), `VariantDeleted` (dispatched by `DeleteVariant`). No LogSubscriber (catalog-structure edits are admin activity, deferred until multi-user — same posture as Media/Taxonomy).

## Tables owned by subsystem

What each subsystem reads from and writes to. Consumers should not write to these tables directly except through the package's actions.

| Subsystem | Tables |
| --- | --- |
| Commerce | `customers`, `customer_groups`, `orders`, `order_lines`, `carts`, `cart_items` |
| Inventory | `stock_items`, `stock_movements`, `stock_reservations` |
| Logging | `domain_logs` |
| Location | `addresses` |
| Media | `media` |
| Payment | `payments`, `payment_profiles`, `webhook_events` |
| Pricing | `prices`, `price_lists`, `vouchers` |
| Purchasing | `suppliers`, `purchase_orders`, `purchase_order_lines` |
| Shipping | `shipments`, `shipment_items` |
| Tax | `tax_rates` |
| Taxonomy | `categories`, `categorizables`, `tags`, `taggables`, `category_morph_counts` |
| Translation | `translations`, `locale_groups` |
| Variants | `options`, `option_values`, `variants`, `option_value_variant`, `optionables` |
| Currency / FlowChain / Storefront / Agent | no tables — enum, orchestration, read-aggregation, or integration layers |

## Subsystems with no periphery

For completeness — when adding to these, also reconsider whether you're adding periphery and update the relevant section above.

- **Currency** — foundational data, no events. Since v0.37.0 it ships one opt-in runtime actor: `Currency\Http\Middleware\SetMoneyDisplayLocale`, registered by consumers on their Filament panel (not auto-registered, fires nowhere unless attached). See External surface → Currency display surface.
- **Location** — provides address data only
- **Storefront** — read-aggregation only, no writes
- **Tax** — configuration/calculation only
- **Translation** — multilingual data tables, no events

---

## External surface (consumer-facing API)

Symbols this package exposes that consumers attach to or call. **Changing any of these is a breaking change for at least one consumer.** Closes the gap the planned `consumer-surface-map.md` was meant to fill — that doc was never written; this section replaces it.

Two consumers today: **in-other-worlds** (`/home/jelte/projects/in-other-worlds`) and **bianka-shop-one** (`/home/jelte/projects/bianka-shop-one`, pre-Phase-1). Each consumer's `docs/periphery.md` mirrors this section from the consumer side ("symbols I depend on"). When a change here lands, audit both consumer periphery docs for impact before releasing.

**Initial population is opportunistic** — derived from `CLAUDE.md`, the bianka BRIEF, and grep-spotting on a few files. The first thorough audit happens on the next periphery-touching brief; refresh "Last verified" at the top of this doc when that happens.

### Capability contracts (`Has*`)

The trait companions (`InteractsWith*`) are documented alongside their contracts in CLAUDE.md "Key Patterns". Both ship together.

| Domain | Contract | Trait | Used by |
| --- | --- | --- | --- |
| Commerce | `HasCart` | `InteractsWithCart` | **package model: `Variants\Variant`** (cart-able unit). in-other-worlds: Product, Bundle. bianka: Product, Bundle, Variant. *Trait now also carries the cart-deletion `deleting` guard (see Model boot hooks).* |
| Inventory | `HasStock` | `InteractsWithStock` | **package model: `Variants\Variant`** (per-variant stock). in-other-worlds: Product, Bundle. bianka: Product, Bundle, Variant |
| Media | `HasMedia` | `InteractsWithMedia` | **package models: `Taxonomy\Category`** (cover image), **`Variants\Variant`**, **`Variants\OptionValue`** (`swatch` collection — single value-level swatch image). in-other-worlds: Content, Product, Bundle. bianka: Product, Bundle |
| Pricing | `HasPrices` | `InteractsWithPrices` | **package model: `Variants\Variant`** (per-variant price). in-other-worlds: Product, Bundle. bianka: Product, Bundle, Variant |
| Purchasing | `HasPurchases` (extends `HasStock`) | `InteractsWithPurchasing` | **Contract + trait ship (released v0.38.0).** Marks a catalog model as purchasable (inventory bought into it). Contract methods: static `purchasableTitleColumn()`, `toPurchaseLineData()` (both trait-defaulted to `name`/`sku`), plus the inherited `HasStock` surface. Trait-only helpers (not on the contract): the reverse `purchaseLines()` relation, plus **`incomingPurchaseLines()`** (MorphMany scoped to Ordered/PartiallyReceived orders — eager-load `purchaseOrder` for a breakdown) and **`incomingQuantity(): int`** (Σ outstanding qty across open orders). The purchase-line picker discovers implementers by walking the morph map. in-other-worlds: Product. bianka: Product/Variant (planned) |
| Storefront | `HasStorefrontPresence` | `InteractsWithStorefrontPresence` | in-other-worlds: Product, Bundle. bianka: Product, Bundle |
| Taxonomy | `HasCategories`, `HasTags` | `InteractsWithCategories`, `InteractsWithTags` | **package model: `Taxonomy\Category` adopts `HasTags`** (intra-domain; enables a `featured`-style category flag, semantics consumer-side). in-other-worlds: Content, Product, Bundle. bianka: Product, Bundle |
| Translation | `HasTranslations` | `InteractsWithTranslations` | **package models: `Variants\Option`** (name), **`Variants\OptionValue`** (label) — column-translation. bianka: Product, Bundle, Category, Tag |
| Translation | `HasLocaleGroup` | `InteractsWithLocaleGroup` | in-other-worlds: Product, Bundle (deliberately not adopted in bianka — see bianka BRIEF §4.9) |
| Variants | `HasVariants` | `InteractsWithVariants` | **Contract + trait ship (unreleased).** Methods: `variants`, `options` (declared axes), `hasVariants`, `lowestVariantPrice`, `hasVariantInStock`, `variantStockTotal` (all trait-defaulted). No public `HasOptions` contract — owner→option link is internal. bianka: Product (planned). in-other-worlds: not adopted — flat SKUs only |

**Adding/removing/renaming a contract or trait** breaks every consumer that opts in. Adding a new method to an existing contract is breaking unless the trait provides a default. Removing or retyping a method is always breaking.

### Public action signatures called by consumers

Actions consumers invoke directly. Constructor + `__invoke` signature is the contract.

| Action | Domain | Called from |
| --- | --- | --- |
| `Inventory\Actions\ReserveStock` | Inventory | in-other-worlds: `App\Actions\Checkout\Steps\ReserveItems`. bianka: planned for checkout FlowChain `ReserveItems` step. **Behaviour (unreleased, non-breaking):** when `reservedUntil` is omitted it now defaults from `inventory.reservation_ttl` (was passed through as `null`) so a Pending reservation always has a TTL the expiry cron can see (G9). Explicit `reservedUntil` wins; config `null` = permanent hold |
| `Inventory\Actions\ConfirmReservation` | Inventory | in-other-worlds: indirectly via `OrderStatusChanged` listener `SyncInventoryOnOrderStatusChange` (now package-side) |
| `Inventory\Actions\ReleaseReservation` | Inventory | in-other-worlds: `App\Listeners\HandlePaymentFailed`. **Signature (unreleased, additive):** `__invoke(StockReservation $reservation, ?string $description = null, bool $onlyIfExpired = false)`. `onlyIfExpired` (used only by the expiry cron) re-checks under the lock that the row is *still* Pending and past its TTL before releasing — so a payment confirmation landing between the cron's unlocked id-snapshot and the locked release can't release a paid reservation (oversell). Default false keeps the permissive Pending\|Confirmed behaviour the payment-fail/admin-cancel/status-change callers rely on. |
| `Inventory\Actions\AdjustStock` | Inventory | in-other-worlds: admin only (`StockMovementResource`) |
| `Commerce\Cart\Http\Support\ResolveCurrentCart` | Commerce | in-other-worlds: `App\Http\Middleware\HandleInertiaRequests::cartSummary()`. bianka: planned for the same. |
| `Commerce\Order\Actions\ResolvePreOrderAudience` | Commerce | `__invoke(Model $purchasable): Collection<PreOrderRecipient>` — distinct pre-order recipients (guests + customers) for a purchasable, deduped by normalized email, shipment-agnostic. in-other-worlds: planned for the `PreOrderReleased` notification + Filament pre-order broadcast (pre-order engagement brief). |
| `Pricing\Actions\CalculateTotal` | Pricing | **Signature CHANGED (unreleased, breaking):** `__invoke(array $items, Currency $currency, int $shippingCost = 0, ?PriceList $priceList = null, ?string $voucherCode = null, ?TaxMode $taxMode = null): PriceBreakdown`. The single `int $taxRate` param is **gone**; each `items[]` entry now carries `taxRateBps`. Tax is gross-inclusive, derived per rate bracket. **Behaviour change (unreleased, non-breaking, 2026-06-04):** `PriceBreakdown.tax` now also includes the VAT contained in `shippingCost` — shipping is a taxable supply, apportioned across the goods' brackets by gross share under the EU ancillary rule (G4); `total` is unchanged (shipping was already gross-inclusive). Per-bracket discount allocation is now largest-remainder, not floor-and-dump-on-highest-rate (G5). in-other-worlds: `App\Actions\Checkout\Steps\CalculateTotals` — update in the same release window. bianka: no checkout yet → TODO. |
| `Pricing\Actions\CalculateIncludedTax` | Pricing | **New (unreleased):** `__invoke(int $grossAmount, int $rateInBasisPoints): int` — the VAT contained in a gross amount. Used by the refund tax-reversal path (R9) too. |

Returns the `Commerce\Order\DTOs\PreOrderRecipient` DTO (`email`, `name`, `locale`, `customerId`) — also part of the surface. Pairs with the `OrderLine::scopePreOrder()` query scope (public).

**Pricing DTO + tax-mode surface changes (unreleased, with the gross-inclusive VAT work):**
- `Pricing\DTOs\PriceBreakdown` gained `taxBreakdown` (`list<TaxBreakdownLine>`, per-rate `{rateBps, taxableBase, tax}`) and `taxMode`; `subtotal` is now **gross** under inclusive mode and `tax` is the *contained* tax (a component, not an addend). New params are defaulted (additive), but the semantic shift is breaking for anything that assumed add-on-top.
- `Pricing\DTOs\PriceBreakdownLine` gained `taxRateBps` (defaulted).
- New `Pricing\DTOs\TaxBreakdownLine` and `Pricing\Enums\TaxMode` (Inclusive/Exclusive — Exclusive throws "not implemented", the B2B seam). Config: `pricing.default_tax_mode` (default `inclusive`); `Pricing\PricingConfig::defaultTaxMode()`.
- `Commerce\Order\Models\Order` gained `taxSummary(): list<TaxBreakdownLine>` (reads the new nullable `orders.tax_summary` JSON column) and stores per-bracket VAT there. `order_lines.tax_amount` is now nullable and **no longer populated** (tax is summarised per bracket on the order); order lines keep their own `tax_rate_bps`.

Variants actions (consumer Filament / admin flows call these; Phase 3 wraps them in `VariantsSchema`):

| Action | Domain | Signature |
| --- | --- | --- |
| `Variants\Actions\CreateVariant` | Variants | `__invoke(Model&HasVariants $owner, array $optionValueIds = [], ?string $sku = null, ?int $position = null): Variant` — validates one-value-per-option + options-declared-on-owner, copies the owner's price template, dispatches `VariantCreated` |
| `Variants\Actions\GenerateVariants` | Variants | `__invoke(Model&HasVariants $owner, array $valueIdsByOption): Collection<Variant>` — declares axes, generates the cartesian product, skips existing combinations |
| `Variants\Actions\CreateDefaultVariant` | Variants | `__invoke(Model&HasVariants $owner): ?Variant` — flat-owner migration; carries price + stock to one default variant; null if already has variants |
| `Variants\Actions\DeleteVariant` | Variants | `__invoke(Variant $variant): void` — deletes variant + owned price/stock/media rows; cart-deletion guard fires first; dispatches `VariantDeleted` |

Purchasing actions (admin / dashboard flows; the Filament Resources will wrap these — retrofit pending, not yet consumer-wired):

| Action | Domain | Signature |
| --- | --- | --- |
| `Purchasing\Actions\CreatePurchaseOrder` | Purchasing | `__invoke(Supplier $supplier, array $lines, ?Currency $currency = null, ?DateTimeInterface $expectedDeliveryAt = null, int $shippingCost = 0, int $customsCost = 0, ?string $notes = null, ?string $reference = null): PurchaseOrder` — creates a Draft + lines in one transaction; `subtotal` = Σ net line costs, `total` += shipping + customs; dispatches `PurchaseOrderCreated`. Each line: `quantity_ordered`, `unit_cost` (net cents) required; optional `purchasable` (snapshots description/sku), `description`/`sku`, `input_vat`, `tax_category` |
| `Purchasing\Actions\PlacePurchaseOrder` | Purchasing | `__invoke(PurchaseOrder $order): PurchaseOrder` — Draft → Ordered, stamps `ordered_at`; dispatches `PurchaseOrderPlaced` |
| `Purchasing\Actions\ReceiveItems` | Purchasing | `__invoke(PurchaseOrder $order, array $quantities, ?string $source = 'dashboard'): PurchaseOrder` — `[lineId => qty]`; validates all quantities before mutating, drops `StockMovement(Received)` per stockable line (referencing the line) and bumps cached `quantity_received`, recomputes status (PartiallyReceived/Received); dispatches `ItemsReceived`. Throws `ReceiveExceedsOutstandingException` / `InvalidPurchaseOrderTransitionException` |
| `Purchasing\Actions\CancelPurchaseOrder` | Purchasing | `__invoke(PurchaseOrder $order): PurchaseOrder` — → Cancelled; does NOT reverse already-received stock; rejects a fully-received order; dispatches `PurchaseOrderCancelled` |

### Currency display surface (v0.37.0)

Money display became locale-aware: `Currency::format()` renders per the resolved display locale (CLDR separators **and** symbol placement) instead of hardcoded period/symbol-first. Resolution: explicit arg > `DisplayLocale` per-request override > `currency.display_locale` config (ships `null`) > app locale. **Output for `en` is byte-identical to pre-0.37 output**, so single-locale `en` consumers see no change until they introduce another locale.

| Symbol | Change | Notes |
| --- | --- | --- |
| `Currency\Enums\Currency::format(int $amount, ?string $locale = null)` | additive param | All consumer call sites keep working; pass `$locale` explicitly in queued/non-request contexts (mail uses the order's stored `locale`) |
| `Pricing\Models\Price::formattedAmount(?string $locale = null)` | additive param | delegates to `Currency::format()` |
| `Support\Filament\MoneyFields::percentLabel(int $bps, ?string $locale = null)` | additive param + locale-aware output | `de` → `7,5 %`; ambient resolution needs a booted app — pass explicit locale in container-less contexts |
| `Currency\Support\DisplayLocale` | new | set/clear/resolve; per-request override used by the middleware |
| `Currency\Http\Middleware\SetMoneyDisplayLocale` | new | consumers register on their Filament panel **with `isPersistent: true`** (Livewire AJAX saves only replay persistent middleware — non-persistent registration renders the wrong separator on the post-save response); resolves money display locale from `Accept-Language`; never touches `app()->setLocale()` |
| `config('currency.display_locale')` | new key | ships `null` = follow app locale |
| `ext-intl` | new composer requirement | already transitively required via filament/support |

Storefront API `formatted` fields (`PriceResource`, `CartResource`, `CartItemResource`) now vary with the request's app locale. They are display strings — consumers must not parse them (verified: neither consumer does). `MoneyFields::formatCents`/`dehydrateCents` stay period-hardcoded machine formats (input state for `type="number"` widgets) — see the guard rail in `MoneyFields`' docblock.

### Public events dispatched (consumer-subscribable)

Listed in full under "Events dispatched (public surface — consumers may subscribe)" above. Each is a `final readonly class` — changing constructor params is a breaking change for any consumer subscribing to that event.

Consumer subscriptions today:

| Event | Subscribed by |
| --- | --- |
| `Payment\Events\PaymentSucceeded` | in-other-worlds `App\Listeners\HandlePaymentSucceeded` |
| `Payment\Events\PaymentFailed` | in-other-worlds `App\Listeners\HandlePaymentFailed` |

Other events have no consumer-side subscribers today — they're routed through package log subscribers only. Adding a consumer-side subscriber to a new event makes that event part of this list.

### Filament Schemas (consumer-embedded form fragments)

Static-factory schemas consumers call from their own Filament Resources. Adding a method is non-breaking; renaming or changing a return type is breaking.

| Schema | Methods consumers call | Used by |
| --- | --- | --- |
| `Media\Filament\MediaSchema` | `fillFormData`, `saveFormData`, `mediaRepeater`, `collectionAllowsCover`, `collections` | in-other-worlds, bianka. **`collectionAllowsCover(string): bool`** reads the new optional per-collection `media.collections.*.cover` flag (default true); when `false` the `mediaRepeater` hides the "Use as cover image" toggle and `saveFormData` refuses to persist that collection as the cover (an embed/document URL is never a valid `<img>` source). in-other-worlds sets `cover => false` on `embed` |
| `Pricing\Filament\PricingSchema` | static factories | in-other-worlds, bianka |
| `Taxonomy\Filament\TaxonomySchema` | static factories | in-other-worlds, bianka |
| `Inventory\Filament\InventorySchema` | `stockFields`, static factories | in-other-worlds, bianka |
| `Translation\Filament\TranslationSchema` | `fillFormData`, `saveFormData` | bianka (in-other-worlds uses HasLocaleGroup pattern instead). **`saveFormData` throws `InvalidArgumentException` on a non-scalar field value** — the value column is a string, so a caller must pass *dehydrated* form state (a RichEditor's raw `$this->data` is a TipTap doc array, not the HTML string). |
| `Variants\Filament\VariantsSchema` | `axesField`, `variantsRepeater`, `fillFormData`, `saveFormData` | bianka (planned). Declares axes + edits existing variants' SKU/price/stock. **Generating new combinations + the owner-price cascade are consumer-side** — call the `GenerateVariants` action from a Filament Action against the real product form (deferred to Phase 5). |
| `Purchasing\Filament\PurchasingSchema` | `purchaseLinesRepeater`, `purchaseLineFields`, `discoverPurchasableModels`, `buildPurchasableOptions` | Used by the package's own `PurchaseOrderResource`. The product picker **discovers** purchasable models by walking the morph map + filtering `HasPurchases` (no per-schema model list); options keyed `{alias}:{id}` to avoid cross-type id collisions. Consumers don't call it directly — they just implement `HasPurchases` on a catalog model. |

### Filament Resource authorization — default-deny base (T-SEC2)

Every Filament Resource this package ships extends `Support\Filament\PackageResource` (not `Filament\Resources\Resource` directly). The base sets `$shouldCheckPolicyExistence = false`, which flips Filament's stock **silent-allow-when-no-policy** to **deny**: an ability with no matching policy method resolves through the Gate and defaults to denied. A `Gate::before` blanket grant (the consumer super-admin bypass) is still honoured; if a consumer enables Filament's global strict-authorization mode, a missing policy throws instead of denying (also fail-closed).

**Consumer requirement:** each package model whose Resource you want reachable in your panel needs a policy that *grants* the relevant abilities. No policy ⇒ no access. Policy mappings a consumer must register (`Gate::policy(...)`) for full admin coverage:

| Package model | Resource | Domain |
| --- | --- | --- |
| `Commerce\Order\Models\Order` | `OrderResource` | Commerce |
| `Commerce\Customer\Models\Customer` | `CustomerResource` | Commerce |
| `Commerce\Customer\Models\CustomerGroup` | `CustomerGroupResource` | Commerce |
| `Pricing\Models\Voucher` | `VoucherResource` | Pricing |
| `Purchasing\Models\PurchaseOrder` | `PurchaseOrderResource` | Purchasing |
| `Purchasing\Models\Supplier` | `SupplierResource` | Purchasing |
| `Tax\Models\TaxRate` | `TaxRateResource` | Tax |
| `Taxonomy\Models\Category` | `CategoryResource` | Taxonomy |
| `Taxonomy\Models\Tag` | `TagResource` | Taxonomy |
| `Variants\Models\Option` | `OptionResource` | Variants |

in-other-worlds ships policies for the first nine (via `AbstractBooleanPolicy` + `can_manage_*` flags); it does not adopt Variants, so `Option` needs no policy there. **A consumer that adopts Variants (bianka) must add an `Option` policy or `OptionResource` is hidden.** A consumer that extends a package Resource (e.g. in-other-worlds' `OrderResource extends` the package one) inherits default-deny transitively — no consumer code change needed, only the policy must exist.

Adding a new Filament Resource to the package? Extend `PackageResource`, add its model to this table, and it is pinned by `PackageResourceAuthorizationTest`'s census.

### Filament page helpers (consumer-extended)

| Symbol | Consumer use |
| --- | --- |
| `Support\Filament\SyncsManualFormState` (trait) | Filament Create/Edit pages `use` it, call `rememberDehydratedFormState($data)` from `mutateFormDataBeforeCreate`/`BeforeSave`, then feed `manualFormState()` to the manual-sync schemas' `saveFormData` in `afterCreate`/`afterSave`. Guarantees schemas get **dehydrated** state, not raw `$this->data` (RichEditor doc-array trap). Falls back to `$this->form->getState()` if `rememberDehydratedFormState` was never called. Used by: bianka (Product). in-other-worlds (planned, all manual-sync pages). |
| `Support\Filament\SavesTranslatableForm` (trait) + `Support\Filament\FormSync` (DTO) | Higher-level helper composing `SyncsManualFormState`: a Create/Edit page `use`s the trait and implements `syncSchemas(): array<FormSync>` (each `FormSync` declares the form `keys` it owns, an optional `fill` closure, and a `save` closure). The trait wires every lifecycle hook (fill → strip-columns → dehydrated save) so a translatable page reduces to one method **and** the RichEditor/TipTap dehydration trap is closed by construction. Package pages using it: Category/Tag/Option Create+Edit. Consumers with translatable resources (bianka Product) can adopt it. Renaming the trait, `FormSync`, or `syncSchemas()` breaks adopters. |

Renaming the trait or its two protected methods breaks every page that uses it.

### FlowChain published surface

Published chains and their step payload contracts are public API — consumers `flowchain:publish` a chain, override `steps()`, and depend on every existing step's `@reads` / `@writes` shape.

| Published chain | Step payload contract | Used by |
| --- | --- | --- |
| `Commerce\Cart\FlowChains\AddToCartChain` | see chain definition | in-other-worlds publishes a copy at `app/Project/FlowChains/Cart/AddToCart.php`, inserts `RecordCartItemAttribution` step |

Adding a step at the end of a chain is non-breaking. Inserting in the middle, renaming, removing, or changing a step's payload `@writes` field is breaking. See [`src/FlowChain/README.md`](../src/FlowChain/README.md) §Publishing.

### Consumer-side shape-parity files (Part C)

Files that exist in every consumer in roughly the same shape, wrapping package APIs. When the underlying package API changes, all of these need parallel updates — a checklist for cross-consumer audits.

| Consumer file shape | Wraps | in-other-worlds path | bianka path (planned) |
| --- | --- | --- | --- |
| `ClaimGuestCart` action | guest-cart claim/merge on login | `app/Actions/Cart/ClaimGuestCart.php` | `app/Actions/Cart/ClaimGuestCart.php` |
| `HandlePaymentSucceeded` listener | confirms reservation, advances order, sends mail | `app/Listeners/HandlePaymentSucceeded.php` | planned in Phase 3 |
| `HandlePaymentFailed` listener | releases reservations, advances order to Cancelled | `app/Listeners/HandlePaymentFailed.php` | planned in Phase 3 |
| `ReserveItems` checkout step | calls `ReserveStock` on each cart line | `app/Actions/Checkout/Steps/ReserveItems.php` | planned in Phase 3 |
| `HandleInertiaRequests` cartSummary | calls `ResolveCurrentCart` | `app/Http/Middleware/HandleInertiaRequests.php` | planned in Phase 1 |
| `OrderConfirmation` mail | rendered from Order + lines | `app/Mail/OrderConfirmation.php` | planned in Phase 3 |

When any of these is edited in one consumer, check whether the other needs the same change. When the underlying package API shifts, audit all rows.

### Logging actor surface (consumer-facing, unreleased)

The audit-actor work makes a slice of the Logging domain consumer-facing: a consumer establishes the request actor so package-emitted audit rows aren't anonymous.

| Symbol | Consumer use |
| --- | --- |
| `Logging\LogContext::setActor(LogActor)` / `actor()` / `forgetActor()` | Consumer `SetAuditActor` middleware sets the boundary actor per web/admin request. Resolved as a container singleton. |
| `Logging\DTOs\LogActor` (factories `user`/`guest`/`gateway`/`system`/`agent`/`unknown`) | The value the consumer passes — `user(id, name)` for an authed admin/customer, `guest()` for anonymous. |
| `Logging\Enums\LogActorType` (User/Gateway/System/Agent) | The queryable bucket on `domain_logs.actor_type`. |

Renaming/retyping any of these is breaking for the consumer middleware. The dispatcher's precedence (explicit > ambient > unknown) means a consumer that sets nothing still gets `unknown` rows, never null.

### What is NOT part of the external surface

- Internal model classes intended for swap via registry (`Currency.php`, `Commerce.php`, `Taxonomy.php`) — consumers configure via `config/{domain}.php` model keys; the registry method names are public, the resolved class names are not.
- Migrations — applied by Laravel, not called.
- Internal `Domain/Exceptions/` subclasses — only the parent `{Domain}Exception` and the specific exception types in the "Exception strategy" CLAUDE.md section are public. Other exception classes can be added/renamed freely.
- Test factories — package-internal even though they ship with the package.
- `protected` / `private` methods on any class, even in a publicly-extended trait.
