# The package had NO CI at all until 2026-08-23 — its ~1,160 tests ran only on
# whatever machine happened to cut a release, while both consumers gate their
# deploys on tests. This closes that.
name: CI

on:
  pull_request:
  push:
    branches: [main]

permissions:
  contents: read

jobs:
  test:
    runs-on: ubuntu-latest
    timeout-minutes: 15
    strategy:
      fail-fast: false
      matrix:
        db: [sqlite, mysql]
    name: test (${{ matrix.db }})

    services:
      mysql:
        image: mysql:8.0
        env:
          MYSQL_ROOT_PASSWORD: root
          MYSQL_DATABASE: shops_test
        ports:
          - 3306:3306
        options: >-
          --health-cmd="mysqladmin ping -h 127.0.0.1 -uroot -proot"
          --health-interval=10s
          --health-timeout=5s
          --health-retries=10

    steps:
      - uses: actions/checkout@v6

      - name: Set up PHP
        uses: shivammathur/setup-php@v2
        with:
          php-version: '8.3'
          extensions: mbstring, pdo, pdo_sqlite, pdo_mysql, bcmath, intl, zip, gd
          coverage: none
          tools: composer:v2

      - name: Cache Composer packages
        uses: actions/cache@v5
        with:
          path: vendor
          key: composer-${{ runner.os }}-${{ hashFiles('composer.lock') }}
          restore-keys: composer-${{ runner.os }}-

      - name: Install PHP dependencies
        run: composer install --no-interaction --no-progress --prefer-dist

      - name: Run tests (SQLite)
        if: matrix.db == 'sqlite'
        run: vendor/bin/phpunit

      # The MySQL leg is not redundant. This package SHIPS migrations, and
      # SQLite cannot validate MySQL DDL — foreign-key names are schema-global
      # and DDL is non-transactional there, which is how a table-rebuild
      # migration took bianka down on 2026-07-09. SQLite also does not enforce
      # UNSIGNED, which is how a fatal negative-delta upsert in
      # MaintainCategoryCounts shipped green (every category detach failed on
      # MySQL).
      #
      # TWO things must hold for this leg to reach MySQL, and only the first is
      # visible here: phpunit.xml pins DB_CONNECTION WITHOUT force="true" so a
      # real env var wins, AND tests/TestCase.php::defineEnvironment must honour
      # that env var instead of hardcoding sqlite. It hardcoded sqlite until
      # 2026-09-02, so this leg silently ran SQLite from the day it was added.
      # If you touch either, re-verify by pointing DB_PORT at a dead port — the
      # suite MUST error, not pass.
      - name: Run tests (MySQL)
        if: matrix.db == 'mysql'
        env:
          DB_CONNECTION: mysql
          DB_HOST: 127.0.0.1
          DB_PORT: 3306
          DB_DATABASE: shops_test
          DB_USERNAME: root
          DB_PASSWORD: root
        run: vendor/bin/phpunit
